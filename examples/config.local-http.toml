workspace_root = "C:/Users/tangerine/.langchain-code-agent"
planner_backend = "langchain"

shell_timeout_seconds = 30
test_command = "python -m pytest -q"
allowed_shell_commands = ["python", "pytest", "rg", "git"]

ignore_patterns = [
  ".git",
  ".venv",
  "__pycache__",
  ".pytest_cache",
  ".mypy_cache",
  "node_modules",
]
